<!-- Copyright (c) 2026 Roronoa & Haruka · Documentation licensed CC BY-NC-SA 4.0 -->
# 原生语音条

> © 2026 Raincove ♡ · Roronoa & Haruka · 文档 [CC BY-NC-SA 4.0](../LICENSE) · 代码 [PolyForm Noncommercial 1.0.0](../LICENSE-CODE)
>
> 把一句话说成企业微信的原生语音条：点开即播、长按转文字的那种语音泡泡。本篇只保留企业微信侧的桥接工艺；这门手艺的正典（Telegram 门、网页播放门、场景感语音的工艺配方与台词学问）在独立仓库 **[voice-for-one](https://github.com/RoronoaHaruka/voice-for-one)**（书名《一副声音》）。

## 先看结论

- 两扇门都能发原生语音条：`msgtype=voice`。素材硬边界是 **AMR 格式、不超过 2MB、不超过 60 秒**，三条都是接口侧强制。
- 链路五步：一句话文本 → TTS → MP3/WAV → 8kHz 单声道 PCM → AMR-NB → `media/upload?type=voice` 拿 `media_id`，应用门走 `message/send`，客服门走 `kf/send_msg`。
- TTS 的 key 永远只住在服务器。

```text
一句话文本
   │  TTS（key 只在服务器）
   ▼
MP3/WAV ─ ffmpeg 解码 → 8kHz 单声道 PCM ─ AMR-NB 逐帧编码 ─┐
                                                              ├─ media/upload?type=voice → media_id
      应用门 message/send(msgtype=voice)  ◄───────────────────┤
      客服门 kf/send_msg(msgtype=voice)   ◄───────────────────┘
```

## 格式这一关：AMR-NB

| 约束 | 值 |
|---|---|
| 容器与编码 | AMR（窄带 AMR-NB，8kHz 单声道），文件头 `#!AMR\n` |
| 大小 | ≤ 2MB |
| 时长 | ≤ 60 秒 |
| 素材有效期 | 临时素材三天过期，`media_id` 不可长存 |

多数发行版的 ffmpeg **解**得动 AMR 却**编**不出来（`libopencore_amrnb` 编码器没编译进去）。最稳的两步走：先用 ffmpeg 把任意音频解到 8kHz 单声道 s16le PCM，再调用 `libopencore-amrnb` 的 C 接口逐帧编码，一帧 160 个样本，12.2kbps（mode 7），开头写上 `#!AMR\n`。独立可跑的转换脚本在 voice-for-one 的 [`to_amr.py`](https://github.com/RoronoaHaruka/voice-for-one/blob/main/code/to_amr.py)。系统依赖两件（Debian 系包名）：

```bash
apt install ffmpeg libopencore-amrnb0
```

如果你的 ffmpeg 恰好带编码器，一条命令等价：

```bash
ffmpeg -i voice.mp3 -ar 8000 -ac 1 -c:a libopencore_amrnb voice.amr
```

## 两扇门怎么发

上传与发送是固定两步：`media/upload?type=voice` 换 `media_id`，再按门发送。语音文件段使用 ASCII 文件名 `voice.amr` 与 `Content-Type: audio/amr`，上传响应的 `type` 必须精确等于 `voice`。应用门 `message/send`，`msgtype=voice`；客服门 `kf/send_msg`，同样的 `msgtype`，并且**占用与文本相同的五格回复预算**、使用稳定 `msgid` 接住网络重试，规则与本仓库文本和文件发送一致。自建应用门没有会话条数额度，只有宽裕的接口频控。语音条经微信插件进入个人微信后，显示为原生语音泡泡，可长按转文字。

腾讯会把 multipart 文件段的 MIME 保存在临时素材记录里，`media/get` 会原样返回该 `Content-Type`。旧 `media_id` 能播、同一 AMR 字节重新上传后只剩嘟声时，先比较旧、新素材的 upload `type` 及 `media/get` 的 `Content-Type`、`Content-Disposition`、`Content-Length` 和 SHA-256；再把同一个新 `media_id` 延迟 30 秒、120 秒重发，判别异步媒体处理竞态。完整探针与公开对照 AMR 在 [voice-for-one 的故障分流章节](https://github.com/RoronoaHaruka/voice-for-one/blob/main/guide/%E4%BC%81%E5%BE%AE%E8%AF%AD%E9%9F%B3%E6%9D%A1.md#%E5%8F%91%E5%87%BA%E5%8E%BB%E6%98%AF%E7%94%B5%E6%B5%81%E5%A3%B0%E6%88%96%E8%BF%9E%E7%BB%AD%E5%98%9F%E5%A3%B0)。

## 参考实现：voice_sender.py

[`../code/voice_sender.py`](../code/voice_sender.py) 把整条链封成一个受控工具：

```bash
# 顺着队列信封原路回一句语音（应用门或客服门自动选）
python3 code/voice_sender.py --envelope /var/lib/wecom-agent/queue/msg_kf_example.json --text "晚安，今天辛苦了。"
# 主动从应用门说一句
python3 code/voice_sender.py --app-user user_example --text "到家了记得说一声。"
```

防护有五层，每层都有对应的消融项（见 [`ABLATION.md`](../tests/ABLATION.md)）：文本长度上限在 TTS 之前拦截（`WECOM_VOICE_MAX_TEXT_CHARS`，先省下生成费再谈别的）；解码后按 PCM 实际时长校验 `WECOM_VOICE_MAX_SECONDS`；编码后按字节校验 `WECOM_VOICE_MAX_BYTES`（只能调小，不能越过 2MB API 上限）；应用门收件人白名单与客服门绑定客户校验沿用全仓库同一套规则。TTS、解码、编码三步都是可注入的函数，离线测试不碰网络也不需要装编码库。

TTS 选型、台词写法、音色与场景感语音的整套工艺见 [voice-for-one](https://github.com/RoronoaHaruka/voice-for-one)。做 voice clone 只有一条不可让步的规矩：**音源必须是你有权使用的**。

## 一段诚实的边界

这条语音链路属于企业微信自建应用与微信客服两扇官方门。至于各种逆向协议的「个人微信机器人」，其原生语音通道在 2026 年年中之后已被服务端封死：消息显示发送成功，对端永远收不到，且无任何报错。走正门，语音条今天依然到达。

## 微信插件里是嘟嘟电流？先换到企业微信客户端核对

2026.9.6 用同一条按本章发出的 AMR-NB 语音做了跨端实测：手机微信里的企微插件能正常播放，电脑版企业微信也能播放，电脑微信里的企微插件却是嘟声或电流。另一位复现者报告 Windows 与 iOS 都嘟声，但没有注明 iOS 端用的是微信插件还是企业微信 App。排查时必须记清实际 App、系统和版本；微信插件的结果不能单独判定上传链路好坏。本次直接确认能播的原生企微端是电脑版企业微信。

换格式无法修补这条插件兼容路径。当晚逐条试过：纯 SILK 上传被 `301017 voice file only support amr` 打回；给 SILK 套一个 `#!AMR\n` 头虽能上传，手机与电脑客户端都不能正确播放；AMR-WB 能上传也不能作为替代；改从微信客服那扇门（`kf/send_msg`）发同一份 AMR-NB，微信插件的结果不变。把同一条语音当 `msgtype=file` 的 MP3 发过去可以调用系统播放器，但显示成文件卡片。

另一组控制实验也说明字节检查有边界：拿确认可播放的入站语音 `media_id` 直接 `message/send` 可以播放；下载同一份 AMR 再经 `media/upload` 上传，回读字节与 SHA-256 完全一致，新 `media_id` 在微信客户端仍可能变成嘟声。原生入站媒体记录与 API 上传媒体记录走了不同的客户端处理路径，单凭下载字节无法复制前者。

排查顺序：先 `ffprobe` 确认 `codec_name=amr_nb`、`sample_rate=8000`、`channels=1`，再用 `ffmpeg -i voice.amr -f null -` 解一遍；随后在原生企业微信客户端里点同一条，本次直接验到的是电脑版企业微信。原生企微能放、微信插件嘟声，上传链路已经工作，当前没有受支持的替代语音格式；电脑上用电脑版企业微信听。原生企微也放不出来，再比较 upload `type`、`media/get` 响应头、SHA-256 与发送延迟，按上一节的探针继续查上传记录。

## 验证清单

- [ ] 生成的 AMR 以 `#!AMR\n` 开头，8kHz 单声道，60 秒与 2MB 以内。
- [ ] 应用门语音条先在原生企业微信客户端里确认能播（本次直接验到电脑版企业微信）；微信插件只作兼容性观察，不作唯一验收端。
- [ ] 客服门语音条到达，且五格回复预算相应减一。
- [ ] 超长文本在 TTS 调用**之前**就被拦下。

## 官方接口索引

- 上传临时素材（voice 限制）：<https://developer.work.weixin.qq.com/document/path/90253>
- 发送应用消息（`voice`）：<https://developer.work.weixin.qq.com/document/path/90236>
- 微信客服发送消息（`voice`）：<https://developer.work.weixin.qq.com/document/path/94677>

以企业微信当前文档与管理后台为准。
